# macOS
.DS_Store

# Python
__pycache__/
*.pyc
.venv/
venv/

# Secrets — API keys / tokens for the selling-ops toolkit
.env

# Large generated/cached data — NOT versioned (see DATA.md to regenerate)
# app/state holds ~1.3 GB of downloaded card images + a chrome profile + cache
app/state/
inputs/
outputs/
export.csv

# TCGplayer exports — re-download from your TCGplayer seller account
TCGplayer__*.csv

# Derived icon build artifacts (source of truth is icon.svg)
AppIcon.icns
icon.iconset/
